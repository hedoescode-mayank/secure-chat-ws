{
  "name": "secure-chat-ws",
  "version": "2.0.0",
  "description": "Professional CLI-based secure chat with room isolation",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "echo 'Launch SecureChat-OS, then visit http://localhost:3000/test.html to run the automated test suite.'"
  },
  "dependencies": {
    "@vercel/analytics": "^2.0.1",
    "express": "^5.2.1",
    "ws": "^8.18.3"
  }
}
